<div style="padding:10px" >
  <%= link_to 'Registrar uma promoção', new_promotion_path %>
</div>

<div class = "bpromocao">
  <%= form_with url: search_promotions_path, method: :get do |form| %>
  <%= form.label :buscar, 'Buscar promoção:' %>
  <%= form.text_field :buscar %>
  <%= form.submit 'Buscar'%>
  <% end %>
</div>

<div class = "bcupom">
  <%= form_with url: search_coupons_path, method: :get do |form| %>
  <%= form.label :buscarcupom, 'Buscar cupom:' %>
  <%= form.text_field :buscarcupom %>
  <%= form.submit "Buscar" %>
  <% end %>
</div>


	<% if @promotions.empty? %>
		<h3> Nenhuma promoção encontrada. </h3>
	<% end %>

<table>
	<tr>
		<th class = "h"> Promoção: </th>
	  <th class = "h"> Descrição: </th>
		<th class = "h"> Desconto: </th>
	</tr>
	<tr>
		<% @promotions.each do |promotion| %>
			<td> <%= link_to promotion.name, promotion %> </td>
			<td> <%= promotion.description %> </td>
			<td> <%= number_to_percentage promotion.discount_rate, separator: ',', precision: 2 %> </td>
	</tr>
		<% end %>
</table>
